#include <bits/stdc++.h>
using namespace std;
/* 
我们知道，每种平衡树都有一种**属于自己的维护树平衡的奇怪操作**，而替罪羊树的奇怪操作就是**暴力重构**。所谓**暴力既优雅**，其时间复杂度仍然是很好的 $O(\log n)$ 。

替罪羊树的每个结点需要存储这些信息：
1. 左右子树编号/指针；
2. 当前结点的值；
3. 以当前结点为根的树的大小和实际大小；
4. 删除标记；


struct node {
	//结点内容
} tzy[maxn], fhq[maxn];  //平衡树名称
int cnt, root;                    //内存池计数器,root为该平衡树的根结点编号
*/

int main() {
    freopen(".in","r",stdin);
    freopen(".out","w",stdout);

    return 0;
}

